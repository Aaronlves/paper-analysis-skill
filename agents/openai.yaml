interface:
  display_name: "Paper Analysis"
  short_description: "Analyze papers with safe handoffs"
  default_prompt: "Use $paper-analysis to analyze this paper, state source access, choose the right depth, reconstruct its arguments, assess evidence, screen its bibliography, and propose handoff candidates without updating files unless authorized."
